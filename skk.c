#include <stdio.h>
int main()
{
    int l;
    int b;
    printf("Enter length of the rectangle= ");
    scanf("%d", &l);
    printf("Enter the breadth of the rectangle= ");
    scanf("%d", &b);
    printf("the area of the given rectangle is = %d", l**b);
    return 0;
}
