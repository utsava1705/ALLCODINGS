#include <stdio.h>
int rev(int arr[n]){
    for(int j =0; j<=5; j++){
        arr[n]= arr[j];
    }
}
int main(){
    int arr[5];
    for(int i=0; i<5; i++){
        printf("enter the %d value : ", i+1);
        scanf("%d", &arr[i]);
    }
    printf("%d\n", rev(arr[i]));
return 0;
}